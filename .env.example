# Real UI target. If omitted, the local mock app is used.
TARGET_BASE_URL=https://test.com
FORM_PATH=/autocomplete-form

# Real API target. If omitted, the local mock API is used.
API_BASE_URL=https://test.com/api
API_RESPONSE_PATH=/responses/98765
API_SUBMISSION_PATH=/responses

# Optional auth token for a real environment.
API_BEARER_TOKEN=
